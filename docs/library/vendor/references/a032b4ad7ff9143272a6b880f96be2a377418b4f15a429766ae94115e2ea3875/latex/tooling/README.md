# LaTeX tooling

This folder is reserved for build helpers and configuration files shared across templates.

Typical contents (as the repo evolves):

- `latexmkrc` templates
- Makefile fragments
- CI build scripts

Template-specific build files should remain inside the template folder.
