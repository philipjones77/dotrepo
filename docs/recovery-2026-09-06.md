# Windows 11 recovery started 2026-09-06

Recovery is in progress following the Windows reinstall. After the restart on
2026-09-07, the preserved Ubuntu disk booted successfully with the original Bash
account, projects and scientific environments. Windows configuration and several
preserved tools are restored, and VS Code connects to WSL. The recorded Windows
application queue and Windows Google Drive sign-in remain incomplete; the
machine is not yet fully restored. WSL Drive startup, GPU computations and TeX
PDF builds have now passed their checks.

Use the [Windows recovery procedure](windows11-recovery.md) for the sequence and
the [maintenance runbook](windows-wsl-maintenance.md) for the shared setup.
The initial other-machine inventory is
[machines/source-2026-09-06](../machines/source-2026-09-06/README.md), pulled in
commit `724c8d5`. Its package lists describe the source computer; the earlier
[maintenance report](maintenance-2026-09-06.md) describes this target before its
Windows reinstall.

The Windows checkout subsequently fast-forwarded through `a8ef2a5` to the other
machine's `b36136b` update, preserving the recovery changes. Its profile and
Terminal improvements were applied with fresh backups. The newer
[September 7 source inventory](../machines/source-2026-09-07/README.md) supersedes
the source package lists below. That source moved Windows Python to standard
CPython; this target's existing Anaconda and scientific environments were
preserved, and its private VS Code override still selects the tested jax-win
interpreter. Fresh normal PowerShell 7 runs Python 3.13.15, R 4.6.1 and Git 2.55.

## Source baseline and target choices

| Area | Source baseline | Recovered target / decision |
| --- | --- | --- |
| Windows | Applications captured on the other computer | Windows 11 Home 25H2, build 26200.9168 |
| Hardware | 32 GiB RAM; RTX 5070 Laptop GPU | 16 GiB physical RAM; RTX 4070 Laptop GPU and Intel UHD Graphics detected; retain hardware-appropriate drivers |
| WSL limits | 16 GiB memory and 24 GiB swap | Applied 8 GiB memory and 4 GiB swap, with a backup of the old configuration |
| Windows Python | Miniconda base: Conda 26.7.2, Python 3.13.13; 129 recorded packages | Preserved the target's Anaconda installation, Python 3.13.15, scientific environments, and project directories; no source-base overlay |
| Node | Native Node 24.20.0 / npm 11.19.0; corepack 0.35.0 | Preserved portable Windows Node 24.20.0, npm 11.19.0 and global corepack 0.35.0; executable and HTTPS checks passed |
| Ubuntu | Ubuntu 24.04; 1,109 APT packages, separate Conda environments and 30 remote extensions inventoried | Original target disk restored and booted; all six non-base environments preserved; all 30 recorded remote extension versions restored |
| Applications | 43 WinGet IDs, plus vendor/Store inventories and 91 Windows extensions | Restore missing applicable applications in reviewed order; package lists include dependencies and are not a request to install every registry record |

The source's CUDA 13.3, GPU/OEM utilities, Visual Studio workloads and scientific
environment manifests need target-specific review. Its Conda manifests are
references for separate environments, not replacements for the target's working
Anaconda base. The source's unresolved local `fftlog-lss` provenance also means
its JAX manifest alone is insufficient to reproduce that environment fully.

The current Windows inventory also confirms VS Code 1.136.1 and WSL 2.7.13.0.
Windows Terminal updated to 1.24.11911.0 after the restart, matching the source.
OneDrive was last observed at 25.087.0506.0001 versus source 26.153.0809.0004;
its update still needs verification. A private gap report covers all 43 source IDs and ten additional
previously recorded target applications; it distinguishes missing registration
from a working standalone installation.

## Verified Windows restoration

- Preserved `C:\dev\dotrepo`, reconciled local work, restored the GitHub SSH
  remote, and pulled main through `2fde13e` on both independent checkouts. SSH/network doctor checks passed.
  Newly restored Git reports `2.55.0.windows.3`. A fresh PowerShell 5 profile
  passes the network doctor using the new Git installation and restored PATH;
  that check no longer depends on the Git executable in Windows.old.
- Recovered VS Code and Windows Terminal preferences, then applied the repository
  bootstrap. Terminal's preferred shell is the recovered Ubuntu Bash account.
  The four console/VS Code PowerShell profiles use the actual Documents location.
  Current-user execution policy was restored to `RemoteSigned`; other policy
  scopes remain unchanged.
- Restored PowerShell 7.6.5 from Microsoft's official per-user ZIP after the
  MSI/elevation attempts did not complete. The download hash and Microsoft
  executable signature were verified, and runtime checks passed. Its executable
  is `~/.local/powershell/current/pwsh.exe`; no system MSI installation is claimed.
- Applied an ignored, machine-specific VS Code override pointing its automation
  shell and named interactive PowerShell profile to the portable executable.
  Updated user PATH while preserving Google Cloud SDK, Node and Conda entries.
  A fresh PowerShell 7 session with its normal profile reports PowerShell 7.6.5,
  Python 3.13.15, Node 24.20.0 and Git 2.55, and passes the network doctor including
  the host override. The normal Windows PowerShell 5 profile also passed.
- Restored editor associations for PDF, images and Markdown, with Markdown diffs
  remaining text. The guarded helper reports the retained Codex 26.901.22334 and
  Claude Code 2.1.263 bundles as already repaired. After the restart, an isolated
  Windows VS Code integration test passed all five assertions: PDF, PNG and
  Markdown viewers, source-line selection and Markdown text diffs. All 91 source
  Windows extensions match their recorded versions. Google's official Antigravity
  extension was added on both hosts, bringing Windows to 92 extensions. The real WSL integration
  passed 12 checks covering the Linux host, configured Python/ipykernel,
  Python/Jupyter/Codex/Claude activation, Bash and code CLI, PDF/PNG/Markdown
  viewers, source-line selection and Markdown text diffs. All 30 source WSL
  extension versions match; the retained WSL Reveal Explorer and added PDF
  Preview and Google Antigravity bring the remote total to 33. The configured Windows project Python
  3.12.12/ipykernel 7.1.0 and WSL Python 3.12.12/ipykernel 7.2.0 execute.
  All isolated test windows were closed. These tests do not establish every
  possible chat-generated link format or desktop file association.
- Preserved the existing Windows Anaconda and portable Node installations.
  Python 3.13.15 and Node 24.20.0 execute successfully; HTTPS checks returned 200.
  Restored the reviewed user PATH entries and `NODE_USE_SYSTEM_CA=1`.
- Copied the target's R 4.6 user library from Windows.old: 137 package directories,
  7,746 files, 268,639,091 bytes, with no copy errors. A real dplyr summarise
  calculation returned 6 using the preserved Windows.old R 4.6.1 executable.
  R 4.6.1 has now been reinstalled and the recovered dplyr/tibble calculation
  passed using the new runtime. WSL system R retains its 29 original packages;
  the source lists 72, so those libraries are not claimed to be exact matches.
- Made optional Google Drive shell startup quiet while its desktop application
  is missing. Explicit Drive commands still report the missing installation.
  Full repository validation and all 15 Windows regression tests passed after
  the source-inventory pull and this startup change.

Old automatic command-approval and workspace-trust bypass preferences were not
imported into the new VS Code installation. No blanket antivirus exclusions were
added as part of restoring shell behavior.

## Cloud SDK, certificates and application data

The standalone Google Cloud SDK was recovered from the target's own Windows.old
profile, along with its local gcloud configuration. SDK copy verification covered
45,015 files and 580,132,793 bytes; all 40 configuration files matched SHA-256
before gcloud ran. Credentials and account identifiers remain private and are
not included in this repository. Authentication refresh has not been tested.

Google Cloud SDK 583.0.0 runs using the retained Anaconda Python 3.13.15.
Its bq and gsutil versions are 2.1.38 and 5.37. The user environment now includes
the SDK's `bin` directory and `CLOUDSDK_PYTHON` pointing to the tested interpreter.

The restored gcloud configuration referenced an old exported certificate bundle.
After backing up the configuration, that stale custom CA reference was removed
and `auth/disable_ssl_validation` was kept false. Online component inventory
passed with certificate validation enabled and reported SDK 583.0.0 as both
installed and latest available. No Cloud SDK update was needed.

Norton 360 is absent from the reinstalled system; Microsoft Defender is active.
Prior Norton certificate repairs are conditional instructions, not settings to
copy blindly. No old Norton CA bundle, TLS bypass, or broad PowerShell/Git
exclusion was restored. App Installer 1.29.290 and its matching WinGet CLI were
repaired, including completing the staged package registration. A subsequent
Store query returned exit 0. This repaired Store failure is not attributed to an
absent Norton installation.

Norton AntiTrack was restored and upgraded to **4.8.7182.14364**, matching
[Norton's latest announced Windows release](https://community.norton.com/t/norton-antitrack-4-8-build-7182-14364-for-windows/526989/1).
The Store catalog offered an older 4.7 package; the 4.8 installer was obtained
from the same vendor CDN using the announced release version, then its exact
product version and valid Gen Digital signature were verified. The installed
version matches and its automatic service is running. Existing-subscription
sign-in and browser-extension setup remain; installation alone does not prove
browser tracking protection is active. No purchase or trial was initiated.

The latest announced Norton 360/Windows security release is
[26.8.11125, delivered through phased LiveUpdate](https://community.norton.com/t/norton-security-26-8-for-windows-is-now-available/527413).
The Store lists an older installer, and Norton's public `latestn360` redirect
returned HTTP 404. A current Norton-account download and activation are still
needed before the suite's version, LiveUpdate and protection can be verified.
The tool action to open that account page was rejected with only “blocked by
policy”; the user was given the official link to open directly. Driver Updater
and Utilities Ultimate remain subject to current download and license checks.

The old Google Drive cache remains in Windows.old. Its roughly 9.0 GB of data
has not been overlaid onto a new live Drive database. Windows Drive 130.0.2.0 is
installed, its executable signature is valid and automatic login startup is
enabled. The new profile still needs Google sign-in; no Windows Drive letter is
mounted yet. Pending/offline data review remains a separate completion check.

## Ubuntu disk and post-restart validation

The preserved target disk has been copied from Windows.old into
`C:\WSL\Ubuntu\ext4.vhdx`. Its size is 200,663,891,968 bytes. The destination is
restricted to the user, SYSTEM and Administrators; original data and private ACL
backups are retained. No distribution was unregistered and no fresh Ubuntu
filesystem was installed over the recovered data.

**Disk preservation completed and verified on 2026-09-07.**
Both complete files are 200,663,891,968 bytes (186.88 GiB); their independently
calculated full SHA-256 hashes match. Source size, timestamps and attributes
were unchanged. The copy process exited with an empty error log. Hash values
and detailed evidence remain in the private preservation receipt.

After the Windows restart, the verified disk was registered in place as Ubuntu
under WSL 2.7.13.0, kernel 6.18.33.2-2. The active registration points only to
`C:\WSL\Ubuntu\ext4.vhdx`. The Windows.old disk was retained through all recovery
checks and subsequently deleted with explicit approval as described below. The
default user is the original `phili`, UID 1000, home `/home/phili`, shell
`/bin/bash`. The native dotrepo checkout fast-forwarded cleanly to `2fde13e`,
bootstrap completed with backups, and its SSH/network doctor passed.

Post-boot preservation checks found all 14 native project directories and all
11 recorded Git repositories. Every older recorded Git HEAD is an ancestor of
the recovered current HEAD; intervening work was retained without resets. All
six non-base Conda history files exactly match their pre-reinstall hashes.
NumPy/SciPy calculations passed in base, bnf-env, jax and uqpy; CPU JAX JIT and
gradient checks passed where JAX is installed. Gemini's Node 26.4.0, GLPK 5.0,
system R 4.6.1 arithmetic and the rqs environment's R/qs round trip also passed.
These initial checks established CPU/runtime preservation; separate GPU checks
are recorded below.
`dpkg --audit` reported no issues.

The canonical writable rclone Drive mount passed independent cloud upload,
readback and deletion checks. A per-user Windows login anchor now keeps the
mount available after ordinary terminals exit. Stop unmounted the drive and
exited the anchor; Start restored it, and duplicate Start left one anchor and one
rclone mount. The canonical Linux lock now serializes Stop with in-flight Start;
an isolated concurrency test passed. Actual login after another Windows restart
has not yet been retested. See [the startup procedure](wsl-google-drive.md).
The Windows Stop action also waits for the retiring anchor mutex before returning,
preventing a lost mount request during an immediate Stop/Start. A real isolated
process/mutex regression covers replacement, timeout and no-anchor behavior;
the previous implementation fails that regression.

A defect in the first startup-helper implementation used forced creation of the
existing HKCU Run key, clearing its values. The helper now creates that key only
when absent and backs up all values before changes. Disposable-key tests confirmed
the original defect on PowerShell 5 and 7 and verify preservation by the fix.
No complete pre-defect Run snapshot was available, so its exact prior contents
cannot be certified. The new WSL anchor and installed Windows Google Drive
startup entries are verified. The older offline user hive supplies historical
startup preferences for applications as they are restored; its OneDrive preference
was disabled and must remain disabled. This issue did not modify HKLM startup,
project data or application credentials.

Signed APT refresh found no pending upgrades. Ten missing practical source tools
were added with 154 packages/dependencies and no removals: Git LFS, Boost, dlib,
FLINT, GMP, MPFR, littler, Python pip/venv and zsh. A C++17 program using those
math libraries compiled and ran; venv/pip, Git LFS, R/littler and zsh checks passed.
Bash remains the default shell. The retained standalone rclone 1.74.2 was kept
instead of replacing it with Ubuntu's older package. Download cache cleanup and
`dpkg --audit` passed. All six Conda history hashes and the base package map stayed
unchanged; source Conda manifests remain separate references, not exact replicas
of these preserved target environments.

The local resume helper was hardened and passed 28 mocked guard checks without
running WSL. It binds the successful preservation receipt to the exact disk
paths, hashes and sizes, requires the post-install Windows restart, checks any
existing registration's directory and VHD name, and preserves the recovered
UID 1000 Bash account. It refuses unexpected repository branches/remotes or
local changes before a fast-forward pull. A previously booted, correctly
registered recovery disk may legitimately have changed since its original hash.
Before the reboot, the live resume command correctly stopped at its restart
guard. Afterward, the same helper completed the import and runtime checks.

Independent Microsoft NTP and two HTTPS Date responses agree with Ubuntu's
synchronized clock. Windows was approximately two hours ahead, with Windows
Time stopped. It was started through normal Windows administrator consent and
resynchronization passed: leap indicator 0, stratum 5, source time.windows.com.
Ubuntu synchronization was preserved. Pre-repair local timestamps can therefore
be misleading. The supported service
and resync commands are documented in Microsoft's
[Windows Time reference](https://learn.microsoft.com/en-us/windows-server/networking/windows-time-service/windows-time-service-tools-and-settings).

## GPU, TeX, AI tools and personal layout

Both Intel UHD and NVIDIA RTX 4070 Laptop display devices report healthy status
and signed drivers. NVIDIA driver 610.47 exposes about 8 GiB VRAM. A Windows CUDA
Driver API test loaded the signed system driver, compiled a small PTX kernel and
verified 4,097 GPU results without installing packages or changing environments.
In WSL, the existing JAX/jaxlib 0.10.1 CUDA 13 environment ran float64 matrix
multiplication and a JIT gradient on the RTX 4070 and matched expected results.
No Linux NVIDIA display driver or replacement CUDA toolkit was needed for these
checks. The source computer's different GPU does not determine this host's driver.

Windows TeX Live 2026 survived under `C:\texlive\2026`; its binary directory was
restored to user PATH with a backup. pdfLaTeX, XeTeX, tlmgr and latexmk run, and a
small LaTeX document compiled to a valid PDF. The older 2025 tree is retained.
WSL's Ubuntu-managed TeX Live 2023 and latexmk 4.83 also compiled a valid PDF;
pdfLaTeX, LuaLaTeX and BibTeX work. XeLaTeX and biber are not installed in WSL.
The working practical TeX baseline was preserved without adding texlive-full.
Real LaTeX Workshop builds and PDF viewers passed four checks on each VS Code
host, including a filename containing spaces. Existing Windows VS Code windows
may need a normal close/reopen to inherit the repaired TeX PATH.

Native WSL Codex 0.153.4, Claude Code 2.1.263 and Antigravity CLI 1.1.27 are
installed and execute. Gemini CLI 0.58.0 was already current. Existing local
ChatGPT and Claude authentication is recognized without copying tokens or sending
test prompts. Windows Claude 1.44121.2, current ChatGPT 26.901.6511.0,
Antigravity hub 2.12.2 and Antigravity IDE 2.5.5 are installed. The current
ChatGPT Store product is `9PLM9XGG6VKS`; the older listing is ChatGPT Classic.
Native Linux ChatGPT preview 26.901.51231 and Antigravity hub 2.12.2 opened
real WSLg windows as UID 1000 with their normal sandbox. The Windows Antigravity
IDE connected to a real Linux extension host using Google's bundled WSL
extension; Bash, native Python/Git and document opening passed. The reusable
`antigravity-ide` Bash launcher was installed with a backup. Account onboarding
and paid model requests remain untested. See [AI tools in WSL](wsl-ai-tools.md).
Windows Gemini Desktop/Start shortcuts open Google's official site in an Edge
app window using Google's icon. This is a web launcher, not a registered PWA or
an unofficial native Gemini application; Google sign-in is unverified.
Google's retired consumer Gemini Code Assist and Gemini CLI Google-login route
were not restored; CLI version checks do not establish consumer authentication.

The old taskbar Favorites format matches the current version. Selected taskbar
preferences, the original Edge/VS Code shortcuts and the original Start menu
layout were recovered from the offline user data. The current layout was backed
up before replacement; registry values and the copied Start layout hash were
verified, and Explorer was refreshed. Original data remains in Windows.old;
applications still awaiting installation can affect which old pins are visible.
The refreshed taskbar was visually checked with Edge and VS Code visible.
The offline user hive was read with a read-only parser after Windows declined to
load its private copy; the whole old hive was never imported into the live account.

For VS Code workspace-only recents, Help > Welcome > Recent shows folders and
workspaces. File > Open Recent / Ctrl+R has no supported folders-only preference;
Welcome's More button returns to that mixed list. No unsupported setting or
history clearing was applied.

## Application queue and remaining work

| Item | Current evidence |
| --- | --- |
| WSL runtime | Restart completed; original Ubuntu registered, booted and checked successfully |
| Git | Installer exit 0; Git 2.55.0.windows.3 executes |
| PowerShell 7 | Official per-user ZIP 7.6.5 restored; verified hash/signature and successful runtime checks |
| App Installer / Store | App Installer/CLI 1.29.290 restored; Store query exit 0 |
| Ordinary recorded apps | VC++ x64/x86 14.51.36247, GitHub CLI 2.100.0, 7-Zip 26.02, SumatraPDF 3.6.1 and Chrome 152.0.7977.83 installed; remaining ordinary apps queued |
| R runtime and RStudio | R 4.6.1 installed and recovered user library checked; signed RStudio 2026.08.2+200 installed per user |
| Google Drive | Windows 130.0.2.0 installed, sign-in pending; WSL writable mount and login anchor checked; Windows.old cache preserved |
| Visual Studio, Docker and CUDA | Larger installations require disk-space and workload review after disk preservation |
| Norton and licensed utilities | Account-based installation/activation and current protection checks remain |
| Microsoft 365 / OneNote | Account-based installation and activation remain |
| Other app accounts | Recheck Google, Box, GitHub Desktop, Claude/Codex and other applicable application sign-ins after installation |

Installations are serialized and account for disk space. The completed disk-copy
receipt recorded 17.25 GiB free. After the reboot, about 24 GiB was available;
later recovery activity reduced this to about 21 GiB. The small-app queue uses a
20 GiB reserve and per-installer space estimates. Larger installations remain
dependent on available space. A reviewed cleanup helper verifies the exact old
Ubuntu disk, preservation receipt, active registration and recovered Linux
checks; its non-deleting review passed. Deleting only that obsolete 186.88 GiB
backup was explicitly approved. The helper re-read its complete original SHA-256,
confirmed the expected hash and live recovered Ubuntu registration, then deleted
only that literal file. It returned exit 0 with `Deleted: true`; the old disk is
absent and the active disk is retained. About 186.88 GiB was recovered, leaving
roughly 209 GiB free before further app installations. Both ordinary app queues
can now proceed within their reserve checks.
Do not treat a queued package, successful download, or bootstrap exit as
proof that an application runs. Recheck application versions, account access,
file viewers, Drive mounts and the Windows/WSL doctors as their dependencies
become available.

The rest of Windows.old and timestamped configuration backups are retained.
Only the explicitly approved old Ubuntu disk was deleted; no additional VHD
compaction or blanket Windows.old cleanup was performed. Raw copy progress,
hashes, installation logs and private
configuration backups stay outside Git; local evidence is under
`.local/recovery-20260906` and the user's `.dotrepo-backups` directory.
