# Legacy Conda wrapper retained for migration. Prefer create-venv.sh (native uv).
name: jax
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - ipykernel
  - gmsh
  - pip:
      - -r requirements.txt
